# e2e/ — Kiểm thử xuyên ứng dụng

Test đầu-cuối chạy qua FE/BO + backend thật, bám
[docs/features/*/test-plan.md](../docs/features/) (vốn bám acceptance criteria trong `spec.md`).

Test đơn vị/tích hợp trong từng app ở cạnh code của app đó; `e2e/` chỉ cho luồng xuyên app.
